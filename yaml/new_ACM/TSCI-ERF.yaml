# Ultralytics YOLO 🚀, AGPL-3.0 license
# RGB-Infrared YOLOv8 with TSCI + ERF

# Parameters
ch: 6
nc: 5

scales:
  s: [0.33, 0.50, 1024]

# YOLOv8.0 backbone
backbone:
  # [from, repeats, module, args]

  - [-1, 1, IN, []]          # 0
  - [-1, 1, Multiin, [1]]    # 1 RGB
  - [-2, 1, Multiin, [2]]    # 2 Infrared

  # -------------------------
  # Visible branch
  # -------------------------
  - [1, 1, Conv, [32, 3, 2]]     # 3-P1/2 visible
  - [-1, 1, Conv, [64, 3, 2]]    # 4-P2/4 visible
  - [-1, 3, C2f, [64, True]]     # 5

  # -------------------------
  # Infrared branch
  # -------------------------
  - [2, 1, Conv, [32, 3, 2]]     # 6-P1/2 infrared
  - [-1, 1, Conv, [64, 3, 2]]    # 7-P2/4 infrared
  - [-1, 3, C2f, [64, True]]     # 8

  # -------------------------
  # P3
  # -------------------------
  - [5, 1, Conv, [128, 3, 2]]    # 9-P3/8 visible
  - [8, 1, Conv, [128, 3, 2]]    # 10-P3/8 infrared

  - [-2, 6, C2f, [128, True]]    # 11-P3/8 visible
  - [-2, 6, C2f, [128, True]]    # 12-P3/8 infrared

  # -------------------------
  # P4
  # -------------------------
  - [-2, 1, Conv, [256, 3, 2]]   # 13-P4/16 visible
  - [-2, 1, Conv, [256, 3, 2]]   # 14-P4/16 infrared

  - [-2, 6, C2f, [256, True]]    # 15-P4/16 visible
  - [-2, 6, C2f, [256, True]]    # 16-P4/16 infrared

  # -------------------------
  # P5
  # -------------------------
  - [-2, 1, Conv, [512, 3, 2]]   # 17-P5/32 visible
  - [-2, 1, Conv, [512, 3, 2]]   # 18-P5/32 infrared

  - [-2, 3, C2f, [512, True]]    # 19-P5/32 visible
  - [-2, 3, C2f, [512, True]]    # 20-P5/32 infrared

  - [-2, 1, SPPF, [512, 5]]      # 21-P5/32 visible
  - [-2, 1, SPPF, [512, 5]]      # 22-P5/32 infrared

  # =====================================================
  # TSCI + ERF
  # TSCI: 选择性交互，输出 cross evidence
  # ERF : RGB / Infrared / Cross 三路证据路由，输出 fused feature
  #
  # TSCI args: [C, embed_dim, small_win, large_win, heads, topk_ratio]
  # 注意：parse_model 中会以 ch[f[0]] 为准修正 C
  # =====================================================

  # -------------------------
  # P3: TSCI + ERF
  # topk_ratio 较小，避免低层背景纹理过度参与
  # -------------------------
  - [[11, 12], 1, TSCI, [128, 64, 4, 7, 4, 0.15]]     # 23 cross P3
  - [[11, 12, -1], 1, ERF, [128]]                     # 24 fused backbone P3

  # -------------------------
  # P4: TSCI + ERF
  # -------------------------
  - [[15, 16], 1, TSCI, [256, 128, 4, 7, 4, 0.25]]    # 25 cross P4
  - [[15, 16, -1], 1, ERF, [256]]                     # 26 fused backbone P4

  # -------------------------
  # P5: TSCI + ERF
  # -------------------------
  - [[21, 22], 1, TSCI, [512, 128, 4, 7, 4, 0.35]]    # 27 cross P5
  - [[21, 22, -1], 1, ERF, [512]]                     # 28 fused backbone P5


# YOLOv8.0 head
head:
  # -------------------------
  # FPN: P5 -> P4
  # -------------------------
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]        # 29
  - [[-1, 26], 1, Concat, [1]]                        # 30 cat fused backbone P4
  - [-1, 3, C2f, [512]]                               # 31

  # -------------------------
  # FPN: P4 -> P3
  # -------------------------
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]        # 32
  - [[-1, 24], 1, Concat, [1]]                        # 33 cat fused backbone P3
  - [-1, 3, C2f, [256]]                               # 34 P3/8-small

  # -------------------------
  # PAN: P3 -> P4
  # -------------------------
  - [-1, 1, Conv, [256, 3, 2]]                        # 35
  - [[-1, 31], 1, Concat, [1]]                        # 36 cat head P4
  - [-1, 3, C2f, [512]]                               # 37 P4/16-medium

  # -------------------------
  # PAN: P4 -> P5
  # -------------------------
  - [-1, 1, Conv, [512, 3, 2]]                        # 38
  - [[-1, 28], 1, Concat, [1]]                        # 39 cat fused backbone P5
  - [-1, 3, C2f, [1024]]                              # 40 P5/32-large

  # 普通检测二选一
  - [[34, 37, 40], 1, Detect, [nc]]                   # 41 Detect(P3, P4, P5)

  # OBB 检测时用这一行，并注释上一行
  # - [[34, 37, 40], 1, OBB, [nc, 1]]                 # 41 OBB(P3, P4, P5)